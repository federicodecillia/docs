---
title: "Delete Source Tag"
api: "DELETE https://app.gptchatbot.it/api/v1/source-tag/{uuid}/delete"
description: "Delete a source tag based on uuid"
---

### Path

<ParamField path="uuid" type="string" required />

### Response

<ResponseField name="success" type="boolean">
  Indicates if api call was successful
</ResponseField>

<RequestExample>

```bash Curl
curl --location --request DELETE 'https://app.gptchatbot.it/api/v1/source-tag/{uuid}/delete' \
--header 'Authorization: Bearer <token>'
```

```py Python
import requests

uuid = '<agent-uuid>'
url = f'https://app.gptchatbot.it/api/v1/source-tag/{uuid}/delete'
headers = {
    'Authorization': 'Bearer <token>'
}

response = requests.delete(url, headers=headers)

if response.status_code == 200:
    print("Request successful! agent deleted.")
else:
    print("Request failed with status code:", response.status_code)
    print(response.text)
```

```ts JavaScript
const axios = require("axios");

const uuid = "<agent-uuid>";
const url = `https://app.gptchatbot.it/api/v1/source-tag/${uuid}/delete`;
const headers = {
  Authorization: "Bearer <token>",
};

axios
  .delete(url, { headers })
  .then((response) => {
    console.log("Request successful! agent deleted.");
  })
  .catch((error) => {
    console.error("Request failed:", error);
  });
```

</RequestExample>

<ResponseExample>

```json Response
{
  "success": true
}
```

</ResponseExample>
